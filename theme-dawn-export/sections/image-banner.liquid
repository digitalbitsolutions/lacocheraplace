{{ 'section-image-banner.css' | asset_url | stylesheet_tag }}

{%- if section.settings.image_height == 'adapt' and section.settings.image != blank -%}
  {%- style -%}
    @media screen and (max-width: 749px) {
      #Banner-{{ section.id }}::before,
      #Banner-{{ section.id }} .banner__media::before,
      #Banner-{{ section.id }}:not(.banner--mobile-bottom) .banner__content::before {
        padding-bottom: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;
        content: '';
        display: block;
      }
    }

    @media screen and (min-width: 750px) {
      #Banner-{{ section.id }}::before,
      #Banner-{{ section.id }} .banner__media::before {
        padding-bottom: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;
        content: '';
        display: block;
      }
    }
  {%- endstyle -%}
{%- endif -%}

{%- style -%}
  #Banner-{{ section.id }}::after {
    opacity: {{ section.settings.image_overlay_opacity | divided_by: 100.0 }};
  }

  {%- if request.page_type == 'page' and page and page.handle == 'como-funciona' and section.id contains 'provider_cta' -%}
    #Banner-{{ section.id }} {
      min-height: 66rem;
    }

    #Banner-{{ section.id }} .banner__content {
      align-items: flex-end;
      justify-content: flex-start;
      padding: 4rem 3.2rem 19.5rem;
    }

    #Banner-{{ section.id }} .banner__box {
      max-width: 55rem;
      background: transparent;
      border: 0;
      box-shadow: none;
      padding: 0;
      margin: 0;
      text-align: left;
    }

    #Banner-{{ section.id }} .banner__heading {
      color: rgb(255, 255, 255);
      font-size: clamp(3.8rem, 3.2rem + 1.3vw, 5.6rem);
      line-height: 1.02;
      letter-spacing: -0.02em;
      margin-bottom: 1.6rem;
    }

    #Banner-{{ section.id }} .banner__text {
      color: rgba(255, 255, 255, 0.93);
      max-width: 52rem;
      margin-bottom: 2.2rem;
    }

    #Banner-{{ section.id }} .banner__text p {
      margin: 0;
      font-size: clamp(1.9rem, 1.7rem + 0.2vw, 2.4rem);
      line-height: 1.45;
    }

    #Banner-{{ section.id }} .banner__buttons .button {
      min-width: 31rem;
      min-height: 5.4rem;
      border-radius: 0;
      background: #0d1117;
      color: #ffffff;
      border: 0;
      font-size: 2rem;
      font-weight: 600;
      letter-spacing: 0;
      padding: 1.4rem 2.4rem;
    }

    #Banner-{{ section.id }} .banner__buttons {
      margin-bottom: 1.8rem;
    }

    #Banner-{{ section.id }} .banner__buttons .button:hover,
    #Banner-{{ section.id }} .banner__buttons .button:focus-visible {
      background: #000000;
    }

    @media screen and (max-width: 989px) {
      #Banner-{{ section.id }} {
        min-height: 54rem;
      }

      #Banner-{{ section.id }} .banner__content {
        padding: 2.4rem 2rem 11rem;
      }

      #Banner-{{ section.id }} .banner__heading {
        font-size: clamp(3rem, 2.4rem + 2vw, 4rem);
      }

      #Banner-{{ section.id }} .banner__text p {
        font-size: 1.9rem;
      }

      #Banner-{{ section.id }} .banner__buttons .button {
        width: 100%;
        min-width: 0;
      }
    }
  {%- endif -%}
{%- endstyle -%}

{%- liquid
  assign full_width = '100vw'
  assign widths = '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840'
  assign use_home_hero_video = false
  assign show_home_service_search = false
  assign home_hero_video_asset = 'video-home-hero.mp4'
  assign home_hero_video_url = ''
  if section.settings.home_hero_video != blank
    assign home_hero_video_url = section.settings.home_hero_video.sources[0].url
  endif
  if request.page_type == 'index' and section.index == 1
    if section.settings.enable_home_hero_video
      assign use_home_hero_video = true
    endif
    assign show_home_service_search = true
  endif

  if section.settings.image_behavior == 'ambient'
    assign half_width = '60vw'
    assign full_width = '120vw'
    assign stacked_sizes = '(min-width: 750px) 60vw, 120vw'
    assign widths = '450, 660, 900, 1320, 1800, 2136, 2400, 3600, 7680'
  elsif section.settings.image_behavior == 'fixed' or section.settings.image_behavior == 'zoom-in'
    assign half_width = '100vw'
    assign stacked_sizes = '100vw'
  else
    assign half_width = '50vw'
    assign stacked_sizes = '(min-width: 750px) 50vw, 100vw'
  endif
  assign fetch_priority = 'auto'
  if section.index == 1
    assign fetch_priority = 'high'
  endif
-%}

<div
  id="Banner-{{ section.id }}"
  class="banner banner--content-align-{{ section.settings.desktop_content_alignment }} banner--content-align-mobile-{{ section.settings.mobile_content_alignment }} banner--{{ section.settings.image_height }}{% if section.settings.stack_images_on_mobile and section.settings.image != blank and section.settings.image_2 != blank %} banner--stacked{% endif %}{% if section.settings.image_height == 'adapt' and section.settings.image != blank %} banner--adapt{% endif %}{% if section.settings.show_text_below %} banner--mobile-bottom{%- endif -%}{% if section.settings.show_text_box == false %} banner--desktop-transparent{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}"
>
  {%- if use_home_hero_video -%}
    <div class="banner__media media{% if section.settings.image_behavior != 'none' %} animate--{{ section.settings.image_behavior }}{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
      <video
        class="banner__media-video"
        autoplay
        muted
        loop
        playsinline
        preload="auto"
        disablepictureinpicture
      >
        {%- if home_hero_video_url != blank -%}
          <source src="{{ home_hero_video_url }}" type="video/mp4">
        {%- else -%}
          <source src="{{ home_hero_video_asset | asset_url }}" type="video/mp4">
        {%- endif -%}
      </video>
    </div>
  {%- elsif section.settings.image != blank -%}
    <div class="banner__media media{% if section.settings.image == blank and section.settings.image_2 == blank %} placeholder{% endif %}{% if section.settings.image_2 != blank %} banner__media-half{% endif %}{% if section.settings.image_behavior != 'none' %} animate--{{ section.settings.image_behavior }}{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
      {%- liquid
        assign image_height = section.settings.image.width | divided_by: section.settings.image.aspect_ratio
        if section.settings.image_2 != blank
          assign image_class = 'banner__media-image-half'
        endif
        if section.settings.image_2 != blank and section.settings.stack_images_on_mobile
          assign sizes = stacked_sizes
        elsif section.settings.image_2 != blank
          assign sizes = half_width
        else
          assign sizes = full_width
        endif
      -%}
      {{
        section.settings.image
        | image_url: width: 3840
        | image_tag:
          width: section.settings.image.width,
          height: image_height,
          class: image_class,
          sizes: sizes,
          widths: widths,
          fetchpriority: fetch_priority
      }}
    </div>
  {%- elsif section.settings.image_2 == blank -%}
    <div class="banner__media media{% if section.settings.image == blank and section.settings.image_2 == blank %} placeholder{% endif %}{% if section.settings.image_2 != blank %} banner__media-half{% endif %}{% if section.settings.image_behavior != 'none' %} animate--{{ section.settings.image_behavior }}{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
      {{ 'hero-apparel-1' | placeholder_svg_tag: 'placeholder-svg' }}
    </div>
  {%- endif -%}
  {%- if section.settings.image_2 != blank -%}
    <div class="banner__media media{% if section.settings.image != blank %} banner__media-half{% endif %}{% if section.settings.image_behavior != 'none' %} animate--{{ section.settings.image_behavior }}{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
      {%- liquid
        assign image_height_2 = section.settings.image_2.width | divided_by: section.settings.image_2.aspect_ratio
        if section.settings.image != blank
          assign image_class_2 = 'banner__media-image-half'
        endif
        if section.settings.image != blank and section.settings.stack_images_on_mobile
          assign sizes = stacked_sizes
        elsif section.settings.image_2 != blank
          assign sizes = half_width
        else
          assign sizes = full_width
        endif
      -%}
      {{
        section.settings.image_2
        | image_url: width: 3840
        | image_tag:
          width: section.settings.image_2.width,
          height: image_height_2,
          class: image_class_2,
          sizes: sizes,
          widths: widths,
          fetchpriority: fetch_priority
      }}
    </div>
  {%- endif -%}
  <div class="banner__content banner__content--{{ section.settings.desktop_content_position }} page-width{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
    <div class="banner__box content-container content-container--full-width-mobile color-{{ section.settings.color_scheme }} gradient">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'heading' -%}
            <h2
              class="banner__heading inline-richtext {{ block.settings.heading_size }}"
              {{ block.shopify_attributes }}
            >
              {{ block.settings.heading }}
            </h2>
          {%- when 'text' -%}
            <div class="banner__text rte {{ block.settings.text_style }}" {{ block.shopify_attributes }}>
              <p>{{ block.settings.text }}</p>
            </div>
          {%- when 'buttons' -%}
            <div
              class="banner__buttons{% if block.settings.button_label_1 != blank and block.settings.button_label_2 != blank %} banner__buttons--multiple{% endif %}"
              {{ block.shopify_attributes }}
            >
              {%- if block.settings.button_label_1 != blank -%}
                <a
                  {% if block.settings.button_link_1 == blank %}
                    role="link" aria-disabled="true"
                  {% else %}
                    href="{{ block.settings.button_link_1 }}"
                  {% endif %}
                  class="button{% if block.settings.button_style_secondary_1 %} button--secondary{% else %} button--primary{% endif %}"
                >
                  {{- block.settings.button_label_1 | escape -}}
                </a>
              {%- endif -%}
              {%- if block.settings.button_label_2 != blank -%}
                <a
                  {% if block.settings.button_link_2 == blank %}
                    role="link" aria-disabled="true"
                  {% else %}
                    href="{{ block.settings.button_link_2 }}"
                  {% endif %}
                  class="button{% if block.settings.button_style_secondary_2 %} button--secondary{% else %} button--primary{% endif %}"
                >
                  {{- block.settings.button_label_2 | escape -}}
                </a>
              {%- endif -%}
            </div>
        {%- endcase -%}
      {%- endfor -%}

      {%- if show_home_service_search -%}
        <div class="home-hero-service-search" data-static-search>
          <form
            action="#"
            method="get"
            role="search"
            class="home-hero-service-search__form"
            id="HeroServiceSearchForm-{{ section.id }}"
          >
            {%- capture hero_service_options_markup -%}
              {%- assign has_service_category_options = false -%}
              {%- for block in section.blocks -%}
                {%- if block.type == 'service_category_option' and block.settings.service_collection != blank -%}
                  {%- assign has_service_category_options = true -%}
                  {%- assign service_option_label = block.settings.option_label | strip -%}
                  {%- if service_option_label == blank -%}
                    {%- assign service_option_label = block.settings.service_collection.title -%}
                  {%- endif -%}
                  <button
                    type="button"
                    class="home-hero-service-search__option"
                    role="option"
                    data-service-value="{{ block.settings.service_collection.url }}"
                    data-service-handle="{{ block.settings.service_collection.handle }}"
                    data-service-label="{{ service_option_label | escape }}"
                    aria-selected="false"
                  >
                    <span class="home-hero-service-search__option-label">{{ service_option_label | escape }}</span>
                    <span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span>
                  </button>
                {%- endif -%}
              {%- endfor -%}
              {%- unless has_service_category_options -%}
                <button type="button" class="home-hero-service-search__option" role="option" data-service-value="/collections/lavado" data-service-handle="lavado" data-service-label="Lavado de coches" aria-selected="false"><span class="home-hero-service-search__option-label">Lavado de coches</span><span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span></button>
                <button type="button" class="home-hero-service-search__option" role="option" data-service-value="/collections/detailing" data-service-handle="detailing" data-service-label="Detailing" aria-selected="false"><span class="home-hero-service-search__option-label">Detailing</span><span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span></button>
                <button type="button" class="home-hero-service-search__option" role="option" data-service-value="/collections/neumaticos-y-llantas" data-service-handle="neumaticos-y-llantas" data-service-label="Neumaticos y llantas" aria-selected="false"><span class="home-hero-service-search__option-label">Neumaticos y llantas</span><span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span></button>
                <button type="button" class="home-hero-service-search__option" role="option" data-service-value="/collections/ppf-wrap-tintado-lunas" data-service-handle="ppf-wrap-tintado-lunas" data-service-label="PPF, wrap y tintado de lunas" aria-selected="false"><span class="home-hero-service-search__option-label">PPF, wrap y tintado de lunas</span><span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span></button>
                <button type="button" class="home-hero-service-search__option" role="option" data-service-value="/collections/chapa-y-pintura" data-service-handle="chapa-y-pintura" data-service-label="Chapa y pintura" aria-selected="false"><span class="home-hero-service-search__option-label">Chapa y pintura</span><span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span></button>
                <button type="button" class="home-hero-service-search__option" role="option" data-service-value="/collections/mantenimiento-ligero" data-service-handle="mantenimiento-ligero" data-service-label="Mecanica basica" aria-selected="false"><span class="home-hero-service-search__option-label">Mecanica basica</span><span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span></button>
                <button type="button" class="home-hero-service-search__option" role="option" data-service-value="/collections/grua-y-auxlio-mecanico" data-service-handle="grua-y-auxlio-mecanico" data-service-label="Grua y auxilio mecanico" aria-selected="false"><span class="home-hero-service-search__option-label">Grua y auxilio mecanico</span><span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span></button>
                <button type="button" class="home-hero-service-search__option" role="option" data-service-value="/collections/parking" data-service-handle="parking" data-service-label="Parking" aria-selected="false"><span class="home-hero-service-search__option-label">Parking</span><span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span></button>
                <button type="button" class="home-hero-service-search__option" role="option" data-service-value="/collections/productos-y-accesorios-para-el-cuidado-automotriz" data-service-handle="productos-y-accesorios-para-el-cuidado-automotriz" data-service-label="Productos y accesorios" aria-selected="false"><span class="home-hero-service-search__option-label">Productos y accesorios</span><span class="home-hero-service-search__option-check" aria-hidden="true">&#10003;</span></button>
              {%- endunless -%}
            {%- endcapture -%}
            <div class="home-hero-service-search__field">
              <span class="home-hero-service-search__icon" aria-hidden="true">
                {{- 'icon-search.svg' | inline_asset_content -}}
              </span>
              <input type="hidden" id="HeroServiceValue-{{ section.id }}" name="service_directory" value="">
              <input type="hidden" id="HeroServiceHandle-{{ section.id }}" name="service_handle" value="">
              <button
                type="button"
                class="home-hero-service-search__trigger"
                id="HeroServiceTrigger-{{ section.id }}"
                aria-haspopup="listbox"
                aria-expanded="false"
                aria-controls="HeroServiceMenu-{{ section.id }}"
                aria-describedby="HeroServiceHelp-{{ section.id }} HeroServiceError-{{ section.id }}"
                aria-invalid="false"
              >
                <span class="home-hero-service-search__trigger-label" id="HeroServiceLabel-{{ section.id }}">
                  Selecciona un servicio para ver proveedores
                </span>
              </button>
              <span class="home-hero-service-search__chevron" aria-hidden="true">&#9662;</span>
              <div
                class="home-hero-service-search__menu"
                id="HeroServiceMenu-{{ section.id }}"
                role="listbox"
                aria-label="Servicios disponibles"
                tabindex="-1"
                hidden
              >
                {{ hero_service_options_markup }}
              </div>
            </div>
            <p class="visually-hidden" id="HeroServiceHelp-{{ section.id }}">
              Usa flecha abajo para abrir la lista. Usa flechas para navegar opciones y Enter para seleccionar.
            </p>
            <p class="home-hero-service-search__error" id="HeroServiceError-{{ section.id }}" role="alert" hidden>
              Selecciona un servicio antes de continuar.
            </p>
            <div class="home-hero-service-search__distance-wrap">
              <div class="home-hero-service-search__distance-head">
                <div class="home-hero-service-search__distance-title">
                  <span class="home-hero-service-search__distance-pin" aria-hidden="true">
                    <svg viewBox="0 0 24 24" role="presentation" focusable="false">
                      <path fill="currentColor" d="M12 2.75c-4.42 0-8 3.5-8 7.83 0 5.48 6.8 10.32 7.09 10.52a1.6 1.6 0 0 0 1.82 0c.29-.2 7.09-5.04 7.09-10.52 0-4.33-3.58-7.83-8-7.83Zm0 10.83a3 3 0 1 1 0-6 3 3 0 0 1 0 6Z"/>
                    </svg>
                  </span>
                  <label class="home-hero-service-search__distance-label" for="HeroDistanceRange-{{ section.id }}">
                    Distancia maxima
                  </label>
                </div>
                <button type="button" class="home-hero-service-search__geo-btn" id="HeroGeoBtn-{{ section.id }}">
                  <span class="home-hero-service-search__geo-icon" aria-hidden="true">
                    <svg viewBox="0 0 24 24" role="presentation" focusable="false">
                      <path fill="currentColor" d="M11 2.6a1 1 0 1 1 2 0v2.02a7.4 7.4 0 0 1 6.38 6.38h2.02a1 1 0 1 1 0 2h-2.02a7.4 7.4 0 0 1-6.38 6.38v2.02a1 1 0 1 1-2 0v-2.02a7.4 7.4 0 0 1-6.38-6.38H2.6a1 1 0 1 1 0-2h2.02A7.4 7.4 0 0 1 11 4.62V2.6Zm1 4.02a5.4 5.4 0 1 0 0 10.8 5.4 5.4 0 0 0 0-10.8Zm0 2.63a2.77 2.77 0 1 1 0 5.54 2.77 2.77 0 0 1 0-5.54Z"/>
                    </svg>
                  </span>
                  Usar mi ubicacion
                </button>
              </div>
              <div class="home-hero-service-search__distance-slider">
                <output class="home-hero-service-search__distance-badge" id="HeroDistanceBadge-{{ section.id }}">10 km</output>
                <input
                  class="home-hero-service-search__distance-range"
                  id="HeroDistanceRange-{{ section.id }}"
                  type="range"
                  min="5"
                  max="100"
                  step="1"
                  value="10"
                  list="HeroDistanceSteps-{{ section.id }}"
                >
                <datalist id="HeroDistanceSteps-{{ section.id }}">
                  <option value="5" label="5 km"></option>
                  <option value="10" label="10 km"></option>
                  <option value="25" label="25 km"></option>
                  <option value="50" label="50 km"></option>
                  <option value="100" label="100 km"></option>
                </datalist>
                <div class="home-hero-service-search__distance-scale">
                  <span class="is-edge-start" style="--distance-stop: 0%;">5 km</span>
                  <span style="--distance-stop: 5.2632%;">10 km</span>
                  <span style="--distance-stop: 21.0526%;">25 km</span>
                  <span style="--distance-stop: 47.3684%;">50 km</span>
                  <span class="is-edge-end" style="--distance-stop: 100%;">100 km</span>
                </div>
              </div>
            </div>
            <input type="hidden" id="HeroDistance-{{ section.id }}" name="distance_km" value="10">
            <input type="hidden" id="HeroUserLat-{{ section.id }}" name="lat" value="">
            <input type="hidden" id="HeroUserLng-{{ section.id }}" name="lng" value="">
            <p class="home-hero-service-search__geo-status" id="HeroGeoStatus-{{ section.id }}" hidden></p>
            <button type="submit" class="home-hero-service-search__button">
              <span>Encuentra proveedores cerca de ti</span>
              <span class="home-hero-service-search__button-arrow" aria-hidden="true">&#8594;</span>
            </button>
          </form>
        </div>
      {%- endif -%}
    </div>
  </div>
</div>

{%- if show_home_service_search -%}
  <script>
    (() => {
      const form = document.getElementById('HeroServiceSearchForm-{{ section.id }}');
      const trigger = document.getElementById('HeroServiceTrigger-{{ section.id }}');
      const label = document.getElementById('HeroServiceLabel-{{ section.id }}');
      const menu = document.getElementById('HeroServiceMenu-{{ section.id }}');
      const valueInput = document.getElementById('HeroServiceValue-{{ section.id }}');
      const handleInput = document.getElementById('HeroServiceHandle-{{ section.id }}');
      const errorMessage = document.getElementById('HeroServiceError-{{ section.id }}');
      const distanceInput = document.getElementById('HeroDistance-{{ section.id }}');
      const distanceRange = document.getElementById('HeroDistanceRange-{{ section.id }}');
      const distanceBadge = document.getElementById('HeroDistanceBadge-{{ section.id }}');
      const geoButton = document.getElementById('HeroGeoBtn-{{ section.id }}');
      const geoStatus = document.getElementById('HeroGeoStatus-{{ section.id }}');
      const userLatInput = document.getElementById('HeroUserLat-{{ section.id }}');
      const userLngInput = document.getElementById('HeroUserLng-{{ section.id }}');
      const options = menu ? Array.from(menu.querySelectorAll('[data-service-value]')) : [];

      if (!form || !trigger || !label || !menu || !valueInput || !handleInput || !options.length) return;
      let activeIndex = 0;

      const closeMenu = () => {
        menu.hidden = true;
        trigger.setAttribute('aria-expanded', 'false');
        form.classList.remove('is-open');
        menu.removeAttribute('aria-activedescendant');
      };

      const openMenu = () => {
        menu.hidden = false;
        trigger.setAttribute('aria-expanded', 'true');
        form.classList.add('is-open');
        const selectedIndex = options.findIndex((item) => item.getAttribute('aria-selected') === 'true');
        setActive(selectedIndex >= 0 ? selectedIndex : activeIndex, true);
      };

      const toggleMenu = () => {
        if (menu.hidden) {
          openMenu();
          return;
        }
        closeMenu();
      };

      const clearInvalid = () => {
        trigger.setAttribute('aria-invalid', 'false');
        form.classList.remove('is-invalid');
        if (errorMessage) {
          errorMessage.hidden = true;
        }
      };

      const setInvalid = () => {
        trigger.setAttribute('aria-invalid', 'true');
        form.classList.add('is-invalid');
        if (errorMessage) {
          errorMessage.hidden = false;
        }
      };

      const setActive = (index, moveFocus) => {
        if (!options.length) return;
        if (index < 0) index = options.length - 1;
        if (index >= options.length) index = 0;
        activeIndex = index;
        options.forEach((item, itemIndex) => {
          if (itemIndex === activeIndex) {
            item.classList.add('is-active');
            item.setAttribute('tabindex', '0');
            menu.setAttribute('aria-activedescendant', item.id);
            if (moveFocus) {
              item.focus();
            }
          } else {
            item.classList.remove('is-active');
            item.setAttribute('tabindex', '-1');
          }
        });
      };

      const setSelection = (option) => {
        options.forEach((item) => item.setAttribute('aria-selected', 'false'));
        option.setAttribute('aria-selected', 'true');
        valueInput.value = option.dataset.serviceValue || '';
        handleInput.value = option.dataset.serviceHandle || '';
        label.textContent = option.dataset.serviceLabel || 'Selecciona un servicio para ver proveedores';
        form.classList.add('has-selection');
        clearInvalid();
      };

      options.forEach((option, index) => {
        option.id = `HeroServiceOption-{{ section.id }}-${index}`;
        option.setAttribute('tabindex', '-1');
        option.addEventListener('click', () => {
          setActive(index, false);
          setSelection(option);
          closeMenu();
          trigger.focus();
        });
        option.addEventListener('mouseenter', () => {
          setActive(index, false);
        });
        option.addEventListener('keydown', (event) => {
          if (event.key === 'ArrowDown') {
            event.preventDefault();
            setActive(activeIndex + 1, true);
          } else if (event.key === 'ArrowUp') {
            event.preventDefault();
            setActive(activeIndex - 1, true);
          } else if (event.key === 'Home') {
            event.preventDefault();
            setActive(0, true);
          } else if (event.key === 'End') {
            event.preventDefault();
            setActive(options.length - 1, true);
          } else if (event.key === 'Enter' || event.key === ' ') {
            event.preventDefault();
            setSelection(option);
            closeMenu();
            trigger.focus();
          } else if (event.key === 'Escape') {
            event.preventDefault();
            closeMenu();
            trigger.focus();
          } else if (event.key === 'Tab') {
            closeMenu();
          }
        });
      });

      trigger.addEventListener('click', toggleMenu);
      trigger.addEventListener('keydown', (event) => {
        if (event.key === 'ArrowDown' || event.key === 'Enter' || event.key === ' ') {
          event.preventDefault();
          openMenu();
        } else if (event.key === 'ArrowUp') {
          event.preventDefault();
          openMenu();
          setActive(activeIndex - 1, true);
        }
        if (event.key === 'Escape') {
          closeMenu();
        }
      });

      document.addEventListener('click', (event) => {
        if (!form.contains(event.target)) {
          closeMenu();
        }
      });

      const showGeoStatus = (message, isError) => {
        if (!geoStatus) return;
        geoStatus.textContent = message;
        geoStatus.hidden = false;
        geoStatus.classList.toggle('is-error', Boolean(isError));
      };

      const requestUserLocation = () => new Promise((resolve, reject) => {
        if (!navigator.geolocation) {
          showGeoStatus('Tu navegador no soporta geolocalizacion.', true);
          reject(new Error('geolocation_not_supported'));
          return;
        }

        if (geoButton) {
          geoButton.disabled = true;
        }

        showGeoStatus('Obteniendo ubicacion...', false);
        navigator.geolocation.getCurrentPosition(
          (position) => {
            userLatInput.value = String(position.coords.latitude || '');
            userLngInput.value = String(position.coords.longitude || '');
            showGeoStatus('Ubicacion detectada. Aplicaremos el filtro por distancia.', false);
            if (geoButton) {
              geoButton.disabled = false;
            }
            resolve(position);
          },
          () => {
            showGeoStatus('Necesitamos tu ubicacion para mostrar proveedores cerca de ti.', true);
            if (geoButton) {
              geoButton.disabled = false;
            }
            reject(new Error('geolocation_denied'));
          },
          { enableHighAccuracy: false, timeout: 8000, maximumAge: 300000 }
        );
      });

      if (geoButton && userLatInput && userLngInput) {
        geoButton.addEventListener('click', () => {
          requestUserLocation().catch(() => {});
        });
      }

      if (distanceRange && distanceInput && distanceBadge) {
        const setDistance = () => {
          const km = Number(distanceRange.value || 10);
          distanceInput.value = String(km);
          distanceBadge.textContent = `${km} km`;
          const min = Number(distanceRange.min || 5);
          const max = Number(distanceRange.max || 100);
          const percent = ((km - min) * 100) / (max - min);
          distanceRange.style.setProperty('--distance-percent', `${percent}%`);
          distanceBadge.style.left = `clamp(4rem, ${percent}%, calc(100% - 4rem))`;
        };
        distanceRange.addEventListener('input', setDistance);
        setDistance();
      }

      form.addEventListener('submit', async (event) => {
        event.preventDefault();
        const targetUrl = valueInput.value;
        const selectedHandle = handleInput.value;

        if (!targetUrl || !selectedHandle) {
          setInvalid();
          openMenu();
          return;
        }
        const nextUrl = new URL(targetUrl, window.location.origin);
        if (distanceInput && distanceInput.value) {
          nextUrl.searchParams.set('distance_km', distanceInput.value);
        }

        if (userLatInput && userLngInput && (!userLatInput.value || !userLngInput.value)) {
          try {
            await requestUserLocation();
          } catch (_error) {
            return;
          }
        }

        if (userLatInput && userLatInput.value && userLngInput && userLngInput.value) {
          nextUrl.searchParams.set('lat', userLatInput.value);
          nextUrl.searchParams.set('lng', userLngInput.value);
        } else {
          showGeoStatus('Necesitamos tu ubicacion para mostrar proveedores cerca de ti.', true);
          return;
        }

        window.location.assign(nextUrl.toString());
      });

      setActive(0, false);
    })();
  </script>
{%- endif -%}

{%- if use_home_hero_video -%}
  {%- style -%}
    #Banner-{{ section.id }} .banner__media-video {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }
  {%- endstyle -%}
{%- endif -%}

{%- if show_home_service_search -%}
  {%- style -%}
    #Banner-{{ section.id }} .home-hero-service-search {
      margin-top: 3.2rem;
      width: min(72rem, 100%);
      margin-inline: auto;
      text-align: center;
    }

    #Banner-{{ section.id }} .home-hero-service-search__form {
      display: block;
      width: min(64rem, 100%);
      margin: 0 auto;
    }

    #Banner-{{ section.id }} .home-hero-service-search__field {
      display: flex;
      align-items: center;
      gap: 1rem;
      width: 100%;
      min-height: 5.8rem;
      border-radius: 999px;
      padding: 0 1.8rem;
      background: rgba(255, 255, 255, 0.97);
      border: 0.1rem solid rgba(17, 24, 39, 0.12);
      box-shadow: 0 0.8rem 2rem rgba(15, 23, 42, 0.22);
      transition: box-shadow 0.2s ease, transform 0.2s ease;
      position: relative;
      isolation: isolate;
      z-index: 2;
    }

    #Banner-{{ section.id }} .home-hero-service-search__form.is-open .home-hero-service-search__field {
      z-index: 30;
    }

    #Banner-{{ section.id }} .home-hero-service-search__form.is-invalid .home-hero-service-search__field {
      border-color: #b91c1c;
      box-shadow: 0 0 0 0.25rem rgba(185, 28, 28, 0.2), 0 0.8rem 2rem rgba(15, 23, 42, 0.22);
    }

    #Banner-{{ section.id }} .home-hero-service-search__field:focus-within {
      box-shadow: 0 1.1rem 2.4rem rgba(15, 23, 42, 0.28);
      transform: translateY(-0.1rem);
    }

    #Banner-{{ section.id }} .home-hero-service-search__icon {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 2rem;
      height: 2rem;
      color: #3f4958;
      flex: 0 0 auto;
    }

    #Banner-{{ section.id }} .home-hero-service-search__icon .icon {
      width: 2rem;
      height: 2rem;
    }

    #Banner-{{ section.id }} .home-hero-service-search__trigger {
      width: 100%;
      border: 0;
      outline: 0;
      background: transparent;
      color: #1f2937;
      font-size: 1.6rem;
      line-height: 1.3;
      padding: 0;
      min-height: 5.2rem;
      cursor: pointer;
      text-align: left;
      font-weight: 500;
      font-family: inherit;
      border-radius: 1.2rem;
    }

    #Banner-{{ section.id }} .home-hero-service-search__trigger:focus-visible {
      outline: 0.25rem solid #1d4ed8;
      outline-offset: 0.15rem;
    }

    #Banner-{{ section.id }} .home-hero-service-search__trigger-label {
      display: block;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      max-width: 100%;
    }

    #Banner-{{ section.id }} .home-hero-service-search__chevron {
      flex: 0 0 auto;
      font-size: 1.3rem;
      color: #6b7280;
      line-height: 1;
      transform: translateY(0.1rem);
      pointer-events: none;
      transition: transform 0.2s ease, color 0.2s ease;
    }

    #Banner-{{ section.id }} .home-hero-service-search__form.is-open .home-hero-service-search__chevron {
      transform: rotate(180deg);
      color: #1d4ed8;
    }

    #Banner-{{ section.id }} .home-hero-service-search__menu {
      position: absolute;
      top: calc(100% + 0.8rem);
      left: 0;
      right: 0;
      max-height: 30rem;
      overflow-y: auto;
      border-radius: 1.6rem;
      background: rgba(255, 255, 255, 0.99);
      border: 0.1rem solid rgba(29, 78, 216, 0.16);
      box-shadow: 0 1.8rem 3.6rem rgba(15, 23, 42, 0.24);
      padding: 0.6rem;
      text-align: left;
      z-index: 40;
      backdrop-filter: blur(10px);
    }

    #Banner-{{ section.id }} .home-hero-service-search__option {
      width: 100%;
      border: 0;
      background: transparent;
      min-height: 4.4rem;
      border-radius: 1.1rem;
      padding: 1rem 1.2rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1.2rem;
      color: #243047;
      cursor: pointer;
      font-size: 1.55rem;
      line-height: 1.3;
      font-family: inherit;
      text-align: left;
    }

    #Banner-{{ section.id }} .home-hero-service-search__option:hover {
      background: #eef4ff;
      color: #173a85;
    }

    #Banner-{{ section.id }} .home-hero-service-search__option:focus-visible,
    #Banner-{{ section.id }} .home-hero-service-search__option.is-active {
      outline: 0.2rem solid #1d4ed8;
      outline-offset: -0.1rem;
      background: #e8f0ff;
    }

    #Banner-{{ section.id }} .home-hero-service-search__option[aria-selected='true'] {
      background: linear-gradient(135deg, rgba(15, 76, 189, 0.1) 0%, rgba(23, 58, 133, 0.12) 100%);
      color: #102b63;
      font-weight: 600;
    }

    #Banner-{{ section.id }} .home-hero-service-search__option-check {
      opacity: 0;
      transform: scale(0.85);
      transition: opacity 0.15s ease, transform 0.15s ease;
      color: #0f4cbd;
      font-size: 1.45rem;
      line-height: 1;
    }

    #Banner-{{ section.id }} .home-hero-service-search__option[aria-selected='true'] .home-hero-service-search__option-check {
      opacity: 1;
      transform: scale(1);
    }

    #Banner-{{ section.id }} .home-hero-service-search__form:not(.has-selection) .home-hero-service-search__trigger-label {
      color: #6b7280;
    }

    #Banner-{{ section.id }} .home-hero-service-search__error {
      margin: 0.8rem auto 0;
      width: min(64rem, 100%);
      text-align: left;
      color: #ffffff;
      background: rgba(153, 27, 27, 0.92);
      border: 0.1rem solid rgba(255, 255, 255, 0.45);
      border-radius: 0.8rem;
      padding: 0.7rem 1rem;
      font-size: 1.35rem;
      line-height: 1.4;
    }

    #Banner-{{ section.id }} .home-hero-service-search__button {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      margin-top: 1.8rem;
      min-height: 6.2rem;
      border-radius: 999px;
      border: 0.2rem solid rgba(255, 255, 255, 0.75);
      padding: 1.3rem 1.6rem;
      font-size: 2.1rem;
      font-weight: 700;
      line-height: 1.3;
      color: #ffffff;
      background: linear-gradient(135deg, #0f4cbd 0%, #173a85 100%);
      box-shadow: 0 1rem 2.4rem rgba(0, 0, 0, 0.28);
      cursor: pointer;
      text-shadow: 0 0.1rem 0.2rem rgba(0, 0, 0, 0.2);
    }

    #Banner-{{ section.id }} .home-hero-service-search__button:hover {
      filter: brightness(1.06);
    }

    #Banner-{{ section.id }} .home-hero-service-search__button span:first-child {
      flex: 1 1 auto;
      text-align: center;
    }

    #Banner-{{ section.id }} .home-hero-service-search__button-arrow {
      width: 5.4rem;
      height: 5.4rem;
      border-radius: 999px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: rgba(12, 49, 133, 0.95);
      font-size: 3rem;
      line-height: 1;
      box-shadow: inset 0 0 0 0.1rem rgba(255, 255, 255, 0.2);
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-wrap {
      margin-top: 1.4rem;
      padding: 1rem 0.2rem 0;
      position: relative;
      z-index: 1;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      flex-wrap: wrap;
      margin-bottom: 0.9rem;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-title {
      display: inline-flex;
      align-items: center;
      gap: 0.8rem;
      color: #ffffff;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-pin {
      width: 2.8rem;
      height: 2.8rem;
      border-radius: 999px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: rgba(255, 255, 255, 0.14);
      color: #ffffff;
      padding: 0.55rem;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-pin svg {
      width: 100%;
      height: 100%;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-label {
      font-size: 1.45rem;
      font-weight: 600;
    }

    #Banner-{{ section.id }} .home-hero-service-search__geo-btn {
      min-height: 4rem;
      border-radius: 999px;
      border: 0.1rem solid rgba(255, 255, 255, 0.5);
      background: transparent;
      color: #ffffff;
      padding: 0.55rem 1.4rem;
      font-size: 1.35rem;
      font-weight: 600;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 0.7rem;
    }

    #Banner-{{ section.id }} .home-hero-service-search__geo-icon {
      display: inline-flex;
      width: 1.9rem;
      height: 1.9rem;
      color: #2f72ff;
    }

    #Banner-{{ section.id }} .home-hero-service-search__geo-icon svg {
      width: 100%;
      height: 100%;
    }

    #Banner-{{ section.id }} .home-hero-service-search__geo-btn:disabled {
      opacity: 0.7;
      cursor: wait;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-slider {
      position: relative;
      padding-top: 2.8rem;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-badge {
      position: absolute;
      top: 0;
      transform: translateX(-50%);
      background: linear-gradient(135deg, #2463dc 0%, #1d4ed8 100%);
      color: #ffffff;
      min-width: 8rem;
      text-align: center;
      border-radius: 1.2rem;
      padding: 0.5rem 0.9rem;
      font-size: 1.2rem;
      font-weight: 700;
      box-shadow: 0 0.8rem 1.8rem rgba(9, 25, 67, 0.35);
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-range {
      --distance-percent: 5%;
      width: 100%;
      -webkit-appearance: none;
      appearance: none;
      height: 0.8rem;
      border-radius: 999px;
      background: linear-gradient(
        to right,
        #2b6fff 0%,
        #2b6fff var(--distance-percent),
        rgba(255, 255, 255, 0.65) var(--distance-percent),
        rgba(255, 255, 255, 0.65) 100%
      );
      outline: none;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-range::-webkit-slider-thumb {
      -webkit-appearance: none;
      appearance: none;
      width: 3.2rem;
      height: 3.2rem;
      border-radius: 999px;
      background: #ffffff;
      border: 0.25rem solid #2b6fff;
      box-shadow: 0 0.6rem 1.5rem rgba(9, 25, 67, 0.35);
      cursor: pointer;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-range::-moz-range-thumb {
      width: 3.2rem;
      height: 3.2rem;
      border-radius: 999px;
      background: #ffffff;
      border: 0.25rem solid #2b6fff;
      box-shadow: 0 0.6rem 1.5rem rgba(9, 25, 67, 0.35);
      cursor: pointer;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-scale {
      position: relative;
      height: 1.6rem;
      margin-top: 0.9rem;
      color: rgba(255, 255, 255, 0.9);
      font-size: 1.15rem;
      line-height: 1.2;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-scale span {
      position: absolute;
      left: var(--distance-stop);
      transform: translateX(-50%);
      white-space: nowrap;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-scale .is-edge-start {
      left: 0;
      transform: none;
    }

    #Banner-{{ section.id }} .home-hero-service-search__distance-scale .is-edge-end {
      left: 100%;
      transform: translateX(-100%);
    }

    #Banner-{{ section.id }} .home-hero-service-search__geo-status {
      margin: 0.6rem 0 0;
      text-align: left;
      font-size: 1.25rem;
      color: #e8f0ff;
    }

    #Banner-{{ section.id }} .home-hero-service-search__geo-status.is-error {
      color: #fecaca;
    }

    @media screen and (max-width: 749px) {
      #Banner-{{ section.id }} .home-hero-service-search {
        margin-top: 2.4rem;
      }

      #Banner-{{ section.id }} .home-hero-service-search__field {
        min-height: 5.1rem;
        padding: 0 1.4rem;
      }

      #Banner-{{ section.id }} .home-hero-service-search__trigger {
        font-size: 1.45rem;
      }

      #Banner-{{ section.id }} .home-hero-service-search__menu {
        top: calc(100% + 0.6rem);
      }

      #Banner-{{ section.id }} .home-hero-service-search__option {
        min-height: 4.2rem;
        font-size: 1.45rem;
      }

      #Banner-{{ section.id }} .home-hero-service-search__button {
        min-height: 5.4rem;
        font-size: 1.8rem;
      }

      #Banner-{{ section.id }} .home-hero-service-search__error {
        font-size: 1.25rem;
      }

    }
  {%- endstyle -%}
{%- endif -%}

{% schema %}
{
  "name": "t:sections.image-banner.name",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.image-banner.settings.image.label"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "t:sections.image-banner.settings.image_2.label"
    },
    {
      "type": "header",
      "content": "Video en home"
    },
    {
      "type": "checkbox",
      "id": "enable_home_hero_video",
      "default": true,
      "label": "Activar video en el hero de home"
    },
    {
      "type": "video",
      "id": "home_hero_video",
      "label": "Video del hero (subido a Shopify Files)"
    },
    {
      "type": "range",
      "id": "image_overlay_opacity",
      "min": 0,
      "max": 100,
      "step": 10,
      "unit": "%",
      "label": "t:sections.image-banner.settings.image_overlay_opacity.label",
      "default": 0
    },
    {
      "type": "select",
      "id": "image_height",
      "options": [
        {
          "value": "adapt",
          "label": "t:sections.image-banner.settings.image_height.options__1.label"
        },
        {
          "value": "small",
          "label": "t:sections.image-banner.settings.image_height.options__2.label"
        },
        {
          "value": "medium",
          "label": "t:sections.image-banner.settings.image_height.options__3.label"
        },
        {
          "value": "large",
          "label": "t:sections.image-banner.settings.image_height.options__4.label"
        }
      ],
      "default": "medium",
      "label": "t:sections.image-banner.settings.image_height.label"
    },
    {
      "type": "select",
      "id": "image_behavior",
      "options": [
        {
          "value": "none",
          "label": "t:sections.all.animation.image_behavior.options__1.label"
        },
        {
          "value": "ambient",
          "label": "t:sections.all.animation.image_behavior.options__2.label"
        },
        {
          "value": "fixed",
          "label": "t:sections.all.animation.image_behavior.options__3.label"
        },
        {
          "value": "zoom-in",
          "label": "t:sections.all.animation.image_behavior.options__4.label"
        }
      ],
      "default": "none",
      "label": "t:sections.all.animation.image_behavior.label"
    },
    {
      "type": "header",
      "content": "t:sections.image-banner.settings.content.content"
    },
    {
      "type": "select",
      "id": "desktop_content_position",
      "options": [
        {
          "value": "top-left",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__1.label"
        },
        {
          "value": "top-center",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__2.label"
        },
        {
          "value": "top-right",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__3.label"
        },
        {
          "value": "middle-left",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__4.label"
        },
        {
          "value": "middle-center",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__5.label"
        },
        {
          "value": "middle-right",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__6.label"
        },
        {
          "value": "bottom-left",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__7.label"
        },
        {
          "value": "bottom-center",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__8.label"
        },
        {
          "value": "bottom-right",
          "label": "t:sections.image-banner.settings.desktop_content_position.options__9.label"
        }
      ],
      "default": "middle-center",
      "label": "t:sections.image-banner.settings.desktop_content_position.label"
    },
    {
      "type": "select",
      "id": "desktop_content_alignment",
      "options": [
        {
          "value": "left",
          "label": "t:sections.image-banner.settings.desktop_content_alignment.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.image-banner.settings.desktop_content_alignment.options__2.label"
        },
        {
          "value": "right",
          "label": "t:sections.image-banner.settings.desktop_content_alignment.options__3.label"
        }
      ],
      "default": "center",
      "label": "t:sections.image-banner.settings.desktop_content_alignment.label"
    },
    {
      "type": "checkbox",
      "id": "show_text_box",
      "default": true,
      "label": "t:sections.image-banner.settings.show_text_box.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.image-banner.settings.mobile.content"
    },
    {
      "type": "checkbox",
      "id": "stack_images_on_mobile",
      "default": true,
      "label": "t:sections.image-banner.settings.stack_images_on_mobile.label"
    },    
    {
      "type": "select",
      "id": "mobile_content_alignment",
      "options": [
        {
          "value": "left",
          "label": "t:sections.image-banner.settings.mobile_content_alignment.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.image-banner.settings.mobile_content_alignment.options__2.label"
        },
        {
          "value": "right",
          "label": "t:sections.image-banner.settings.mobile_content_alignment.options__3.label"
        }
      ],
      "default": "center",
      "label": "t:sections.image-banner.settings.mobile_content_alignment.label"
    },
    {
      "type": "checkbox",
      "id": "show_text_below",
      "default": true,
      "label": "t:sections.image-banner.settings.show_text_below.label"
    }
  ],
  "blocks": [
    {
      "type": "heading",
      "name": "t:sections.image-banner.blocks.heading.name",
      "limit": 1,
      "settings": [
        {
          "type": "inline_richtext",
          "id": "heading",
          "default": "t:sections.image-banner.blocks.heading.settings.heading.default",
          "label": "t:sections.image-banner.blocks.heading.settings.heading.label"
        },
        {
          "type": "select",
          "id": "heading_size",
          "options": [
            {
              "value": "h2",
              "label": "t:sections.all.heading_size.options__1.label"
            },
            {
              "value": "h1",
              "label": "t:sections.all.heading_size.options__2.label"
            },
            {
              "value": "h0",
              "label": "t:sections.all.heading_size.options__3.label"
            },
            {
              "value": "hxl",
              "label": "t:sections.all.heading_size.options__4.label"
            },
            {
              "value": "hxxl",
              "label": "t:sections.all.heading_size.options__5.label"
            }
          ],
          "default": "h1",
          "label": "t:sections.all.heading_size.label"
        }
      ]
    },
    {
      "type": "text",
      "name": "t:sections.image-banner.blocks.text.name",
      "limit": 1,
      "settings": [
        {
          "type": "inline_richtext",
          "id": "text",
          "default": "t:sections.image-banner.blocks.text.settings.text.default",
          "label": "t:sections.image-banner.blocks.text.settings.text.label"
        },
        {
          "type": "select",
          "id": "text_style",
          "options": [
            {
              "value": "body",
              "label": "t:sections.image-banner.blocks.text.settings.text_style.options__1.label"
            },
            {
              "value": "subtitle",
              "label": "t:sections.image-banner.blocks.text.settings.text_style.options__2.label"
            },
            {
              "value": "caption-with-letter-spacing",
              "label": "t:sections.image-banner.blocks.text.settings.text_style.options__3.label"
            }
          ],
          "default": "body",
          "label": "t:sections.image-banner.blocks.text.settings.text_style.label"
        }
      ]
    },
    {
      "type": "buttons",
      "name": "t:sections.image-banner.blocks.buttons.name",
      "limit": 1,
      "settings": [
        {
        "type": "header",
        "content": "t:sections.image-banner.blocks.buttons.settings.header_1.content"
        },
        {
          "type": "text",
          "id": "button_label_1",
          "default": "t:sections.image-banner.blocks.buttons.settings.button_label_1.default",
          "label": "t:sections.image-banner.blocks.buttons.settings.button_label_1.label",
          "info": "t:sections.image-banner.blocks.buttons.settings.button_label_1.info"
        },
        {
          "type": "url",
          "id": "button_link_1",
          "label": "t:sections.image-banner.blocks.buttons.settings.button_link_1.label"
        },
        {
          "type": "checkbox",
          "id": "button_style_secondary_1",
          "default": false,
          "label": "t:sections.image-banner.blocks.buttons.settings.button_style_secondary_1.label"
        },
        {
        "type": "header",
        "content": "t:sections.image-banner.blocks.buttons.settings.header_2.content"
        },
        {
          "type": "text",
          "id": "button_label_2",
          "default": "t:sections.image-banner.blocks.buttons.settings.button_label_2.default",
          "label": "t:sections.image-banner.blocks.buttons.settings.button_label_2.label",
          "info": "t:sections.image-banner.blocks.buttons.settings.button_label_2.info"
        },
        {
          "type": "url",
          "id": "button_link_2",
          "label": "t:sections.image-banner.blocks.buttons.settings.button_link_2.label"
        },
        {
          "type": "checkbox",
          "id": "button_style_secondary_2",
          "default": false,
          "label": "t:sections.image-banner.blocks.buttons.settings.button_style_secondary_2.label"
        }
      ]
    },
    {
      "type": "service_category_option",
      "name": "Categoria buscador hero",
      "settings": [
        {
          "type": "collection",
          "id": "service_collection",
          "label": "Coleccion de servicio"
        },
        {
          "type": "text",
          "id": "option_label",
          "label": "Etiqueta personalizada (opcional)"
        }
      ]
    },
  ],
  "presets": [
    {
      "name": "t:sections.image-banner.presets.name",
      "blocks": [
        {
          "type": "heading"
        },
        {
          "type": "text"
        },
        {
          "type": "buttons"
        }
      ]
    }
  ]
}
{% endschema %}

